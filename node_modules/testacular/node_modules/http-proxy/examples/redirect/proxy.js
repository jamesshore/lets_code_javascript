var http = require('http'),
    httpProxy = require('http-proxy');

var options = {
  http: {
    router: {
      'server1.local': 8001,
      'server2.local': 8002
    }
  },
  https: {
    router: {
      'server1.local': 8001,
      'server2.local': 8002
    }
  }
}
//
// Create your proxy server
//
httpProxy.createServer(options).listen(4000);
httpProxy.createServer(options).listen(4001);
