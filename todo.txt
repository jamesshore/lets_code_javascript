WeeWikiPaint

Minimum Marketable Features:
+ marketing home page (episodes 1-32)
+ single-user painting on home page (episodes 33-200)
+ replace home page with professional design (episodes 201-321)
+ IE 11 support (and get rid of IE 8) (episodes 322-324)
+ Android support (episodes 325-327)
+ iOS 8 support (episode 328)
* replace 404 page with professional design (episodes 329+)
- responsive design
- accessibility
- polish
  - clear button retains focus when drawing (IE8, 9, Chrome)
  - drawing glitches occur when exiting drawing area that is flush against side of browser
  - one-pixel gap on right side of drawing area (Firefox, others unknown)
  - favicon
	- iOS buttons flash rather than depressing
	- IE 9 and IE 11, button goes diagonally rather than vertically (but only for button, not for links)
? multiple painting/erasing tools
- collaborative painting
- save the pages (persistence / databases)
- wiki-fy the pages
- more complex painting?
- more collaborative features?

User Stories to finish current MMF:
* update design
- release

Engineering Tasks:
* Run server code out of generated/dist as well (fix hardcoded path in weewikipaint.js)
- Restore 'it.only()' functionality of client test build (conflicts with grep-based test subsetting)
- Is there a better of handling cross-directory requires (e.g., `require("../shared/_assert.js")` broke)?
- Where do we want our vendor files to live?
- Move hard-coded paths in Jakefile.js to paths.js?

To Do on current task:
- Copy server code into dist directory
- Modify runners to run out of the distribution directory
	- Procfile
	- weewikipaint.js
- Rename weewikipaint.js --> run.js?